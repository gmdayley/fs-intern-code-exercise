/* TODO - implement buildArrayOfNums as a function that takes a single parameter
 * which is the number of elements to create in the array, each element representing
 * a number which is the 1-based index of the element.  
 * 
 * For example:
 * 
 * var result = buildArrayOfNums(3);
 *
 * Should build an array === [1, 2, 3] 
 * 
 * Your implementation shoulp pass all tests located in the mycode.test.js file.
 */

const hasDuplicates = undefined;

export { hasDuplicates };
